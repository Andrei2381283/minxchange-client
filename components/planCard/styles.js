import styled from "styled-components";

export const PlanCardDescr = styled.span`
    margin-top: 1rem;
    text-align: center;
    color: ${props => props.color};
    font-weight: 500;
    font-size: 1.25rem;
`